# DECI.Project - Frontend environment example (local development without Docker)
# Copy this file to frontend/.env. In Docker, this value is supplied as a build
# argument by docker-compose.yml, so this file is only needed for local dev.

VITE_API_BASE_URL=http://localhost:5000/api
